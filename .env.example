# Injected by the free Neon integration in Vercel Marketplace.
DATABASE_URL=
# Gateway uses Vercel OIDC automatically on deployments. Optional local key:
AI_GATEWAY_API_KEY=
# Generation only calls a model whose live input/output price is exactly zero.
QUIZ_AI_ENABLED=true

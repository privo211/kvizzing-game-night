# Injected by the free Neon integration in Vercel Marketplace.
DATABASE_URL=
# Server-side OpenRouter key. The app only calls openrouter/free.
OPENROUTER_API_KEY=
# Generation also checks the live router price is exactly zero before every call.
QUIZ_AI_ENABLED=true

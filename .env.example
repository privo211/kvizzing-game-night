# Injected by the free Neon integration in Vercel Marketplace.
DATABASE_URL=
# Server-side OpenRouter key. The app only calls the pinned zero-price model.
OPENROUTER_API_KEY=
# Generation also checks the live model price is exactly zero before every call.
QUIZ_AI_ENABLED=true
